import type { Pillar } from '../lib/site';

// Stories planned but not written yet. They show on the topic pages as
// "in the works". When a story is published, delete its line here.
export const upcoming: { pillar: Pillar; title: string; note: string }[] = [
  {
    pillar: 'money',
    title: 'Why most retail CFD traders lose money',
    note: 'Being rewritten with sources',
  },
];
